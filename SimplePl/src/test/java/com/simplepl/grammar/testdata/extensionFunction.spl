fun int usingOpenedFile(string fileName) extension int(File fileObj) {
    extension(file);
}

fun int usingOpenedFile2(string fileName) extension void(File fileObj, int fileLength) {
    extension(file);
}

fun int user(){
    usingOpenedFile("1.txt"){
        string data=read(fileObj);//comment
    };

    usingOpenedFile2("1.txt") myfile, fileLength {
        string data=read(myfile);
    };
}